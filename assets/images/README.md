# Images / screenshots

`placeholder.svg` is a generic stand-in used everywhere a real screenshot is
still needed. Replace it per project with your own asset and update the
corresponding `<img src="...">` in `index.html`.

## Screenshots still needed (TODO)

- [ ] `research-scivqa.*` — RE-SciVQA figure or pipeline diagram
- [ ] `rag-coding-conventions.*` — Production RAG system screenshot/diagram
- [ ] `devsecops.*` — vulnerability-alert pipeline / Dependency-Track screenshot
- [ ] `attendance-app.*` — student attendance mobile app screenshot
- [ ] `santander.*` — (optional) ROC/AUC or feature-importance chart

Recommended: export at ~1600px wide, compress (e.g. `pngquant`/`squoosh`),
and keep each under ~200 KB so the page stays fast.
